<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RoomWordSimple" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.RoomWordSimple" parent="Base.Theme.RoomWordSimple" />
    <style name="Theme.RoomWordSample.WithActionBar" parent="Base.Theme.RoomWordSimple">
        <!-- Transparent system bars for edge-to-edge. -->

        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
        <item name="toolbarStyle">@style/Widget.Material3.Toolbar.Surface</item>
    </style>

</resources>